# نظام تصريح الأعمال

## نظرة عامة
نظام متكامل لإدارة تصاريح دخول ومباشرة أعمال التشطيبات للمقاولين والعمال.

## المميزات المنفذة ✅

### 1. صفحة طلب التصريح (`/work-permit`)
- نموذج احترافي شامل يتضمن:
  - بيانات الموقع (تاريخ بداية/نهاية، اسم الموقع، كود، منطقة، مرحلة العمل)
  - بيانات المقاول + رفع صورة الرقم القومي
  - إضافة حتى 9 عمال مع أسمائهم وصور أرقامهم القومية
  - الشروط والالتزامات
  - بيانات المهندس المسؤول
  - ملاحظات إضافية
- تصميم احترافي مع تأثيرات بصرية
- رسالة نجاح بعد الإرسال

### 2. API Endpoint (`/api/work-permit`)
- استقبال البيانات من النموذج
- رفع وحفظ الصور (الرقم القومي للمقاول والعمال)
- حفظ البيانات في ملف JSON
- إنشاء مجلد خاص لكل تصريح

### 3. لوحة التحكم (`/admin/work-permits`)
- عرض جميع التصاريح
- فلترة حسب الحالة (الكل، قيد المراجعة، موافق عليها، مرفوضة)
- عرض تفاصيل كل تصريح
- الموافقة أو رفض التصاريح
- عرض صور الأرقام القومية
- إحصائيات سريعة

### 4. إضافة في الصفحة الرئيسية
- قسم "تصريح أعمال" في الصفحة الرئيسية
- رابط مباشر لصفحة طلب التصريح

### 5. إضافة في لوحة التحكم الرئيسية
- زر سريع للوصول إلى إدارة التصاريح

## المميزات القادمة 🚧

### 1. قاعدة البيانات
- [ ] إنشاء جدول `work_permits` في قاعدة البيانات
- [ ] حفظ البيانات في قاعدة البيانات بدلاً من الملفات
- [ ] إضافة علاقات مع جداول المستخدمين والمواقع

### 2. نظام الموافقات
- [ ] تنفيذ API للموافقة والرفض
- [ ] حفظ بيانات الموافق (اسم، تاريخ، توقيع)
- [ ] إضافة سبب الرفض
- [ ] تاريخ الموافقات والتعديلات

### 3. نظام الإشعارات
- [ ] إشعار للمقاول عند الموافقة/الرفض
- [ ] إشعار للإدارة عند طلب جديد
- [ ] إشعارات WhatsApp/SMS

### 4. طباعة وتصدير
- [ ] تصميم قالب PDF للتصريح
- [ ] طباعة التصريح بعد الموافقة
- [ ] تحميل PDF
- [ ] إضافة QR Code للتحقق

### 5. تحسينات إضافية
- [ ] البحث والفلترة المتقدمة
- [ ] تصدير التقارير (Excel/CSV)
- [ ] لوحة إحصائيات شاملة
- [ ] نظام الأرشفة
- [ ] تجديد التصاريح
- [ ] تتبع حالة التصريح (QR Code Scanner)

## البنية التقنية

### الملفات الرئيسية
```
dlyl-shwqy-2/
├── app/
│   ├── work-permit/
│   │   └── page.tsx                    # صفحة طلب التصريح
│   ├── admin/
│   │   └── work-permits/
│   │       └── page.tsx                # لوحة التحكم
│   └── api/
│       └── work-permit/
│           └── route.ts                # API Endpoint
├── components/
│   └── work-permit-form.tsx            # نموذج التصريح
└── public/
    └── uploads/
        └── work-permits/               # مجلد حفظ الصور
            └── [permitId]/
                ├── contractor_id.jpg
                ├── worker_0_id.jpg
                └── permit_data.json
```

### تدفق البيانات
1. المستخدم يملأ النموذج في `/work-permit`
2. يتم إرسال البيانات إلى `/api/work-permit` (POST)
3. يتم حفظ الصور في `public/uploads/work-permits/[permitId]/`
4. يتم حفظ البيانات في ملف JSON (مؤقتاً)
5. الإدارة تراجع الطلب في `/admin/work-permits`
6. الموافقة/الرفض يتم عبر API

## الاستخدام

### للمقاولين
1. الذهاب إلى الصفحة الرئيسية
2. الضغط على "تصريح أعمال"
3. ملء جميع البيانات المطلوبة
4. رفع صور الأرقام القومية
5. إرسال الطلب
6. انتظار الموافقة

### للإدارة
1. الذهاب إلى لوحة التحكم
2. الضغط على "تصاريح الأعمال"
3. مراجعة الطلبات الجديدة
4. عرض التفاصيل والصور
5. الموافقة أو الرفض
6. طباعة التصريح (قريباً)

## ملاحظات مهمة
- يجب إنشاء قاعدة بيانات قبل الإنتاج
- يجب تأمين رفع الملفات (حجم، نوع)
- يجب إضافة نظام مصادقة للإدارة
- يجب نسخ احتياطي للملفات المرفوعة

## التحديثات الأخيرة
- ✅ إنشاء نموذج التصريح الاحترافي
- ✅ إنشاء API endpoint لاستقبال البيانات
- ✅ إنشاء لوحة التحكم للموافقات
- ✅ إضافة في الصفحة الرئيسية
- ✅ رفع الملفات وحفظها
- ✅ عرض التفاصيل والصور
